{
  "name": "ez-dynamo",
  "version": "0.2.0",
  "description": "A library for easily interacting with DynamoDB, built ontop of the DynamoDB DocumentClient.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint '**/*.js'",
    "format": "prettier --write \"{,!(node_modules)/**/}*.js\""
  },
  "author": "Michael Drury",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.732.0",
    "chai": "^4.2.0",
    "joi": "^17.2.0",
    "mocha": "^8.2.0"
  },
  "devDependencies": {
    "eslint": "^7.7.0",
    "prettier": "^1.19.1"
  }
}
